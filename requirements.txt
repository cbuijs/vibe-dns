
# Core Dependencies
dnspython>=2.6.0
PyYAML>=6.0.1

# Async HTTP Client (DoH/DoH3 support)
httpx>=0.27.0

# Network interface resolution (for bind_interfaces feature)
netifaces>=0.11.0

# Optional: HTTP/3 support (requires system libssl)
# Uncomment if you want DoH3 (DNS-over-HTTP/3) support
# httpx[http3]>=0.27.0
# aioquic>=0.9.25

# Optional: Better performance for HTTP requests
# Uncomment for fallback HTTP client if httpx not available
# requests>=2.31.0

# Optional: Advanced regex support (already using 'regex' in code)
regex>=2023.12.25

# Development Dependencies (optional)
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
# black>=24.0.0
# mypy>=1.8.0

# Performance Monitoring (optional)
# py-spy>=0.3.14
# memory-profiler>=0.61.0

